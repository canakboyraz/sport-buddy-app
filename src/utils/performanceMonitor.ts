export const performanceMonitor = {
    start: (label: string, data?: any) => {
        // console.log(`[Performance] Start: ${label}`, data);
    },
    end: (label: string) => {
        // console.log(`[Performance] End: ${label}`);
    },
};
